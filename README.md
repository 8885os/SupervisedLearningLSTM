# SupervisedLearningLSTM
Forecasting predicted pollution data using an RNN

#### Takes already cleaned data
#### Scales the data 
#### Creates LSTM model
#### Evaluates accuracy of the model with RMSE
